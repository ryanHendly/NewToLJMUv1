<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ryan.newtoljmuv1">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/launcherlogo"
        android:label="New to LJMU"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        android:largeHeap="true"

        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label=" "
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".CampusActivity" />
        <activity
            android:name=".CityCampus"
            android:label="City Campus" />
        <activity
            android:name=".PleasantCampus"
            android:label="Mount Pleasant Campus" />
        <activity
            android:name=".MarshCampus"
            android:label="IM Marsh Campus" />
        <activity
            android:name=".BarsActivity"
            android:label="Bars" />
        <activity
            android:name=".EventsActivity"
            android:label="@string/title_activity_events" />
        <activity
            android:name=".GymsActivity"
            android:label="@string/title_activity_gyms" />
        <activity
            android:name=".ClubActivity"
            android:label="@string/title_activity_club" />
        <activity
            android:name=".RestaurantActivity"
            android:label="@string/title_activity_restaurant" />
        <activity
            android:name=".TouristActivity"
            android:label="@string/title_activity_tourist" />
        <activity
            android:name=".WildCardActivity"
            android:label="@string/title_activity_wild_card"></activity>
    </application>

</manifest>